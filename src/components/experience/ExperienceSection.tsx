// ExperienceCarousel is a "use client" component so Next.js App Router
// automatically creates a client/server boundary when imported here.
// No next/dynamic wrapper needed (ssr:false is not allowed in Server Components).
export { default } from "@/components/experience/ExperienceCarousel";

